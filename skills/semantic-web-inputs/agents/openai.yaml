interface:
  display_name: "Semantic Web Inputs"
  short_description: "Build locale-aware inputs without losing intent"
  default_prompt: "Use $semantic-web-inputs to design and verify this locale-aware input."
